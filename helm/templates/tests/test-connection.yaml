apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Chart.Name }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ .Chart.Name }}-service:{{ .Values.fastapi_service_clusterip.port }}"]
  restartPolicy: Never
